name: deploy-staging
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: deploy
        env:
          API_TOKEN: ${{ secrets.STAGING_API_TOKEN }}
        run: echo deploying
